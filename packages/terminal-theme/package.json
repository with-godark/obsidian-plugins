{
  "name": "@godark/terminal-theme",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node --env-file=../../.env ./build.mjs",
    "clean": "rm -rf dist",
    "dev": "node --env-file=../../.env ./build.mjs --watch",
    "dev:sync": "node --env-file=../../.env ./build.mjs --watch --sync",
    "lint": "stylelint \"src/**/*.css\"",
    "sync": "node --env-file=../../.env ./build.mjs --sync"
  }
}
